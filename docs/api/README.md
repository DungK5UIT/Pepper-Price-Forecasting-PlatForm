# API Documentation

Placeholder. The Java backend is the platform's only public API surface
(see `docs/architecture/overview.md`); its contract (resources,
versioning, auth, error format) will be documented here once the first
endpoints are designed.

This directory does not document the internal API between the Java
backend and the Python ML service — that's an implementation detail
between two components owned by the same team, not a public contract.
