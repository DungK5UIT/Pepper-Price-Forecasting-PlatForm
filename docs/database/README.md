# Database Documentation

Placeholder. The conceptual domain model lives in
`docs/architecture/domain-model.md`. This directory will document the
actual PostgreSQL schema — tables, constraints, indexes — once migrations
exist in `db/migrations/`, and should stay in sync with them as the schema
evolves.
