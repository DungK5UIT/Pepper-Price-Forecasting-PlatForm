# Development Setup

Placeholder. No component has code to run yet (Phase 0 — foundation only).

This document will cover, as each part lands:
- Prerequisites (JDK, Node, Python versions; Docker)
- Running PostgreSQL/Redis locally (via `infra/docker/`)
- Running the backend, frontend, and ML service individually
- Running the full stack together
- Running tests for each component
