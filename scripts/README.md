# Scripts

Operational and developer-convenience scripts that don't belong to any
single component (e.g. spinning up the full local stack, one-off data
backfills, release helpers).

**Does not own**: application logic — a script that becomes a real,
recurring part of the product's behavior belongs in the owning component
instead, not here.

Empty until a real recurring operational need exists.
